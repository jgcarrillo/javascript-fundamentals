const nombre = 'Deadpool';
const real = 'Wade Winston';

const normal = nombre + ' ' + real;

// Todo lo que se escriba dentro de ${} ejecuta código JS
const template = `${nombre} ${real}`;

console.log(normal);
console.log(real);

// Ofrece la posibilidad de desarrollo multilínea
const html = `
<h1>Hola Mundo</h1>
<p>Esto es una prueba</p>
`;

console.log(html);
